You are analyzing a GraphQL API discovered from "{{ app_name }}".

An enum type "{{ enum_name }}" was found with these values: {{ values_json }}

Provide a JSON response:
{
  "description": "What this enum represents in business terms"
}