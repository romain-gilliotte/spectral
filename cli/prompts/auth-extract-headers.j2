You are analyzing HTTP traces from a web application. Your task is to identify which request header names carry authentication (tokens, session cookies, API keys, etc.).

Use the query_traces tool to inspect the request headers of one trace. Look for headers like Cookie, X-Auth-Token, X-API-Key, X-Session, or any custom header that appears to carry auth credentials.

Do NOT include standard non-auth headers like Content-Type, Accept, User-Agent, Origin, Referer, etc.

One query should be enough. If no auth headers are present, return an empty list — do not keep searching.

Return ONLY the header names that carry authentication.