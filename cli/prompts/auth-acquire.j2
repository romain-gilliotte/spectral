Generate a Python `acquire_token()` function that performs the full authentication flow for this web API.

`acquire_token()` takes NO arguments — use `prompt_text` and `prompt_secret` to get user credentials.
It must handle the FULL flow (all steps: request OTP, then verify, etc.), fully headless via HTTP requests — never ask the user to open a browser.
