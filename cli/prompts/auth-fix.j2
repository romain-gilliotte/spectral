The script failed with the following error:

### Traceback
{{ exception | format_script_traceback }}

### Script stdout

{% if script_stdout %}
{{ script_stdout }}
{% else %}
No debug() output was captured. Add `debug()` calls to log HTTP response status codes, headers, and body excerpts so you can diagnose the issue.
{% endif %}

Fix the script so it works. You can use `query_traces` to re-examine the captured traffic and verify the exact auth flow (URLs, headers, redirect chains, response bodies). Add `debug()` calls at every HTTP call to log the status code, relevant headers (especially `Location`), and the first 500 characters of the response body. Their output will be shown to you if the script fails again.

Return ONLY the corrected Python code in a ```python block.
