Generate a Python `refresh_token(current_refresh_token)` function that exchanges the given refresh token for new auth credentials.

**Exception to the shared rules**: you may infer a standard OAuth2 token refresh endpoint (e.g., `/oauth/token`) even if it does not appear in the captured traces.
